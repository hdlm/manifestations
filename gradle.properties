# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
#org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# debugging jvm args
org.gradle.jvmargs=-Xms128M -Xmx2192M -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:+UseCompressedClassPointers -XX:MaxMetaspaceSize=1536M -XX:+UseParallelOldGC -XX:MaxGCPauseMillis=200 -XX:GCTimeRatio=99 -XX:-UseAdaptiveSizePolicy -XX:AdaptiveSizeDecrementScaleFactor=4 -Didea.kotlin.plugin.use.k2=true -XX:+UnlockDiagnosticVMOptions -XX:NativeMemoryTracking=detail -Dkotlin.daemon.jvm.options="-Xms320M,-Xmx1024M,-Dfile.encoding=UTF-8,-XX:+UseParallelGC,-XX:+UseCompressedClassPointers,-XX:MaxMetaspaceSize=1024M,-XX:+UseParallelOldGC,-XX:MaxGCPauseMillis=200,-XX:GCTimeRatio=99,-XX:-UseAdaptiveSizePolicy,-XX:AdaptiveSizeDecrementScaleFactor=4,-Didea.kotlin.plugin.use.k2=true,-XX:+UnlockDiagnosticVMOptions,-XX:NativeMemoryTracking=detail"
# relase jvm args
#org.gradle.jvmargs=-Xms2160M -Xmx4160M -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:+UseCompressedClassPointers -XX:MaxMetaspaceSize=1536M -XX:+UseParallelOldGC -XX:MaxGCPauseMillis=200 -XX:GCTimeRatio=99 -XX:-UseAdaptiveSizePolicy -XX:AdaptiveSizeDecrementScaleFactor=4 -XX:+UseLargePages -XX:+UseLargePagesInMetaspace -Didea.kotlin.plugin.use.k2=true -XX:+UnlockDiagnosticVMOptions -XX:NativeMemoryTracking=detail -Dkotlin.daemon.jvm.options="-Xms704M,-Xmx1024M,-Dfile.encoding=UTF-8,-XX:+UseParallelGC,-XX:+UseCompressedClassPointers,-XX:MaxMetaspaceSize=1024M,-XX:+UseParallelOldGC,-XX:MaxGCPauseMillis=200,-XX:GCTimeRatio=99,-XX:-UseAdaptiveSizePolicy,-XX:AdaptiveSizeDecrementScaleFactor=4,-XX:+UseLargePages,-XX:+UseLargePagesInMetaspace,-Didea.kotlin.plugin.use.k2=true,-XX:+UnlockDiagnosticVMOptions,-XX:NativeMemoryTracking=detail"
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true